// Widget tests are not runnable without a real Firebase project.
// After completing Firebase setup (see SETUP.md), you can add integration tests here.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
